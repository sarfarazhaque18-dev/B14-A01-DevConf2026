Act as an expert frontend developer and UI/UX designer. Create **only one standalone section** for my existing **2026 Developer Conference** landing page titled **"AI Sandbox Hackathon."** Do **not** generate a full webpage, navigation bar, footer, hero section, or any global styles. This section should be designed to drop directly into my existing codebase.

The section should feature a **responsive split-screen layout**:

* **Left Side:** Present the hackathon information with a bold heading, a short description, attractive prize cards, a stylish countdown timer placeholder (UI only), event details (team size, duration, difficulty), and a prominent **"Join the Hackathon"** button.
* **Right Side:** Create a premium AI coding playground that resembles a modern code editor or terminal. Include macOS-style window controls, a sample prompt input (e.g., *"Build a responsive dashboard using React"*), a blinking cursor, a **Run Prompt** button, and a simulated AI-generated code output with subtle typing or loading animations.

The design should feel futuristic, interactive, and premium—taking inspiration from GitHub Copilot, OpenAI Playground, Vercel, Linear, and Stripe Sessions. Use glassmorphism, soft gradients, glowing accents, rounded corners, layered shadows, smooth hover effects, and tasteful micro-interactions.

**Important integration requirements:**

* Generate **only this section** (`<section>...</section>`), along with the CSS and minimal JavaScript required for this section.
* Do **not** redefine or modify global styles.
* Use my existing CSS variables (such as `--bg-primary`, `--surface`, `--accent`, `--text-primary`, `--border`, etc.) so the section matches the rest of my website.
* Use unique class names (e.g., prefixed with `sandbox-`) to avoid conflicts with my existing code.
* Make the layout fully responsive and production-ready.
* Ensure the HTML, CSS, and JavaScript are clean, modular, and easy to integrate into an existing project.

The output should contain **only**:

1. HTML for the AI Sandbox Hackathon section.
2. CSS specific to this section.
3. Minimal JavaScript (only if needed for UI animations).

The section should look like a premium feature on a world-class developer conference website, not a generic template.
